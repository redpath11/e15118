#ifndef __HAVE_UTILS_HH
#define __HAVE_UTILS_HH


// Function to check if parameters are valid
// (i.e. != -1)

static inline bool
is_valid(long long& val)
{
  return (val != -1);
};

static inline bool
is_valid(long& val)
{
  return (val != -1);
};

static inline bool
is_valid(int& val)
{
  return (val != -1);
};

static inline bool
is_valid(short& val)
{
  return (val != -1);
};

static inline bool
is_valid(char& val)
{
  return (val != -1);
};

static inline bool
is_valid(float& val)
{
  return (val != -1);
};

static inline bool
is_valid(double& val)
{
  return (val != -1);
};

static inline bool
is_valid(const long long& val)
{
  return (val != -1);
};

static inline bool
is_valid(const long& val)
{
  return (val != -1);
};

static inline bool
is_valid(const int& val)
{
  return (val != -1);
};

static inline bool
is_valid(const short& val)
{
  return (val != -1);
};

static inline bool
is_valid(const char& val)
{
  return (val != -1);
};

static inline bool
is_valid(const float& val)
{
  return (val != -1);
};

static inline bool
is_valid(const double& val)
{
  return (val != -1);
};




#endif
